{
  "name": "klaudii",
  "version": "1.0.0",
  "description": "Web-based manager for Claude Code CLI sessions",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-service": "./install.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:contracts": "vitest run test/contracts/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "express": "^5.1.0",
    "qrcode": "^1.5.4",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^4.0.18"
  },
  "optionalDependencies": {
    "@google/gemini-cli-a2a-server": "^0.32.1"
  }
}
